# 方法与证据边界

## 研究起点

严格起点为 `2026-08-13T13:02:03.901Z`，即 `2026-08-13 21:02:03（北京时间）`。

仓库创建时间早于或等于起点的项目不会作为“发布后新项目”纳入主观察清单。维护者仍可在单独的背景资料中引用它们，但不能混进发布后趋势样本。

## 三层证据状态

### 已确认

仓库名称、描述或 GitHub Topic 明确指向 `DeepSeek Harness` / `deepseek-harness`，并且存在插件、桌面端、工具、部署、视觉、渠道等实现信号；或者 README 明确说明“这是为 DeepSeek Harness 构建的实现”。

如果完整名称只是在 README 中被顺带提及，而仓库身份和直接实现关系不清晰，自动任务只会放入“待复核”，不会直接进入主榜。

“已确认”只确认相关性和公开实现入口，不等于：

- 已在本机安装运行；
- 已通过安全审计；
- 已验证所有 README 声明；
- 已达到生产可用标准。

### 待复核

仓库名与描述只使用 `DSH` 缩写，或属于 fork，尚不足以自动证明独立功能。这类项目保留在 `data/candidates.json`，不进入公开排名。

### 排除

包括明显误命中、教程/白皮书/纯清单、起点前项目、无实现证据项目和维护者 denylist。denylist 优先级最高，并会把历史已确认项目或候选项目移出对应清单。

## 发现方式

每小时任务查看最近 7 天创建的仓库，默认使用：

- `"deepseek harness" in:name,description,readme`
- `deepseek-harness in:name,description,readme`
- `topic:deepseek-harness`
- `topic:dsh-plugin`
- `dsh-plugin in:name,description`

搜索结果会受到 GitHub 索引延迟、每次返回上限和 README 读取上限影响。默认每次最多读取 80 个新仓库 README；其余结果只基于元数据判断，证据不足时进入待复核。对漏检仓库使用 `config/manual-allowlist.json` 补充。

## 排名

### 当前关注度

```text
70 × log10(stars + 1) + 30 × log10(forks + 1)
```

使用对数是为了避免一个头部项目完全压扁其他项目。Issues 不进入分数，因为未关闭 Issue 既可能代表活跃，也可能代表缺陷，含义不稳定。

### 观察窗口变化

```text
stars_delta = 当前 stars - 上一个快照 stars
forks_delta = 当前 forks - 上一个快照 forks
rank_change = 上一个快照排名 - 当前排名
```

`rank_change > 0` 表示排名上升。页面使用真实快照间隔，不把 20 小时或 30 小时的间隔硬称为精确 24 小时。

### 动量排序

```text
10 × stars_delta + 25 × forks_delta
```

动量分只是传播和二次开发信号的代理，不是长期增长率。至少需要两个快照才能生成。

## 功能分类榜

每个已确认项目先按仓库名称、简介和 Topics 进入一个功能分类。规则按具体信号优先，例如插件市场优先于泛化 UI 信号；未命中明确规则时进入“其他实现型扩展”。自动分类可以通过源数据复核，但不代表项目只能属于一个方向，也不是功能完整性验证。

分类榜提供三个互补视角，不再发明新的综合分：

```text
Stars 总量榜 = 分类内所有项目当前 Stars 之和
项目数量榜 = 分类内已确认项目数量
窗口增长榜 = 分类内可比较项目 stars_delta 之和
```

窗口增长榜只有在至少存在两个真实快照时才启用，并展示真实时间间隔。暂无任何可比项目的新分类保留为空，不会被误记为零增长或参与窗口排名。分类 Top 3 沿用项目关注度分排序，用来解释该分类主要由哪些项目构成；CSV 还保留项目占比、Stars 占比、Forks 总量和可比较项目数。

Stars 总量可能被一个头部仓库显著拉高，项目数量也可能受拆分仓库方式影响。因此分类榜描述的是公开规模、拥挤度和观察窗口变化，不是质量、成熟度或商业价值排序。

## 快照策略

每次成功运行会写入一个按上海日期与小时命名的明细快照，例如 `2026-08-15-13.json`。同一小时内手动重复运行会刷新该小时文件，不会伪造多个更细的观察点。

同一份数据还会刷新 `data/archive/YYYY-MM-DD.json` 的当天长期归档。默认只保留最近 14 天的小时明细；更早的小时文件仅在对应日归档已存在时才会删除。排名构建会按 `snapshot_at` 去重，因此同一数据同时存在于小时明细与日归档时只算一个观察点。旧版 `data/snapshots/YYYY-MM-DD.json` 日快照继续兼容。

对短暂网络错误、429、部分 5xx 和明确限流响应，更新器会进行最多 3 次、单次等待不超过 5 秒的有限重试。最终仍失败时，GitHub Actions 不会提交任何改动，因此远端仍保留上一次成功版本。

本地手动运行如果在生成过程中失败，可能留下未提交文件；应先查看 `git status`，不要把失败运行产生的局部文件直接提交。

## 维护者公开所在地

地区字段来自仓库 owner 对应 GitHub 用户或组织账号公开填写的 `location`。系统按确定性词表分为“国内（中国大陆）”“中国港澳台”“海外”“未知”：

- 只在文本明确包含国家、地区、省份或城市时归类；
- 空值、`Earth`、`Remote` 等非地理文本归为未知；
- 不根据姓名、用户名、头像、使用语言、邮箱域名或仓库内容推断；
- 同一 owner 只请求一次，成功结果默认缓存 30 天；新 owner 在下一次小时更新时自动补充；
- 地点字段表示账号公开所在地，不代表国籍、公司注册地、仓库归属或全部贡献者分布。

自由文本词表无法覆盖全球所有写法，因此“未知”是正常且刻意保留的状态。分类器变化后可运行 `npm run normalize` 重新计算已有缓存，不必重新请求 GitHub。

## 中文简介

GitHub 原始 `description` 始终保留。若原文包含中文，页面直接使用原文；纯英文文本按“仓库名 + 完整原文”匹配 `data/translations.json`：

1. 缓存命中时直接复用，不调用模型。
2. 新项目或原文变化时，小时任务使用仓库内置 `GITHUB_TOKEN` 调用 GitHub Models。
3. 提示词要求只做简体中文翻译、保留技术名词，并把仓库文本当作不可信数据，不执行其中的指令。
4. 返回内容必须是约定 JSON，且译文必须包含中文字符才会写入缓存。
5. 模型不可用、返回异常或配额不足时只记录待翻译状态，不让排名和快照更新失败，也不删除已有译文。

中文译文用于降低阅读门槛，不验证原项目的功能、质量或宣传声明；网页提供英文原文展开入口，CSV 同时输出中英文。

## 可复现与安全边界

- 自动任务只读取 GitHub 公开 API 和 README，不克隆、不安装、不执行被观察项目代码。
- 排名、CSV、页面和推文草稿全部由同一份快照生成；CI 会在构建后检查生成文件是否有未提交差异。
- 页面把 GitHub 描述当作不可信文本转义，并限制可打开链接为 `https://github.com/`。
- 工作流按职责拆分：小时数据任务只获得内容写权限，Pages 部署才获得页面和身份令牌权限。
- Pages 工作流监听小时任务成功完成事件；这是因为 `GITHUB_TOKEN` 产生的机器人推送不会再次触发普通 `push` 工作流。
- GitHub 的计划任务可能延迟或漏跑，尤其在整点高负载时段；因此任务安排在每小时第 17 分钟，并始终使用真实时间差计算变化。
- GitHub Actions 版本由 Dependabot 每周检查，但首次上线后的真实运行结果仍需要维护者在 Actions 中确认。
